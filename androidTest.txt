아하, 지금 코드 보니까 우리가 JS에서 뷰포트 높이를 억지로 잡아버려서 갤럭시 폴드 펼쳤을 때 네비게이션 영역까지 컨텐츠가 뻗어 내려가고 있는 상태야 😭

특히 이 부분이 핵심 문제야:

// index.html
function setAppHeight() {
    const vh = window.visualViewport ? window.visualViewport.height : window.innerHeight;
    document.body.style.height = `${vh}px`;

    console.log(`Viewport Height updated: ${vh}px`);
}

window.addEventListener('resize', setAppHeight);
window.addEventListener('orientationchange', setAppHeight);
window.addEventListener('load', setAppHeight);
if (window.visualViewport) {
    window.visualViewport.addEventListener('resize', setAppHeight);
}


그리고 CSS 쪽에서도 이렇게 되어 있고:

html,
body {
    margin: 0;
    padding: 0;
    width: 100%;
    height: 100%;
    overflow: hidden; /* Prevent scroll on body, let JS handle size */
}


이 두 콤보 때문에:

WebView가 decorFitsSystemWindows(true)로 네비 영역 위까지만 그려주려고 해도

JS가 body.height = vh(px)로 다시 전체 화면 높이(네비 영역 포함)를 강제로 덮어씌움

그 상태에서 폴드 펼치면, nav가 오버레이 모드로 뜨고, 게임 화면은 그 뒤로 깔려버림

✅ 1단계: JS에서 강제 높이 잡는 코드 제거

index.html 맨 아래 쪽에 있는 setAppHeight 부분을 완전히 지워버리거나 이렇게만 남겨줘:

// ==================== 화면 크기 변경 시 레이아웃 강제 업데이트 (Safe Area 대응) ====================
// 예전엔 여기서 body 높이를 강제로 조절했는데,
// 폴더블 + 네비게이션 바랑 충돌해서 모두 제거함.
// 브라우저(WebView)가 제공하는 안전 영역(insets)에만 맡긴다.


즉, 아래 네 줄 + 이벤트 등록 부분 전체 삭제:

function setAppHeight() { ... }
window.addEventListener('resize', setAppHeight);
window.addEventListener('orientationchange', setAppHeight);
window.addEventListener('load', setAppHeight);
if (window.visualViewport) {
    window.visualViewport.addEventListener('resize', setAppHeight);
}

✅ 2단계: html/body 높이 & overflow 정리

styles.css 제일 위쪽 html, body / body 부분을 이렇게 수정해줘. 

styles

(1) html, body 수정

기존:

html,
body {
    margin: 0;
    padding: 0;
    width: 100%;
    height: 100%;
    overflow: hidden; /* Prevent scroll on body, let JS handle size */
}


👉 수정:

html,
body {
    margin: 0;
    padding: 0;
    width: 100%;
    min-height: 100%;
    /* height: 100%; 와 JS 강제 높이 제거
       overflow 제어는 body에서만 하도록 둠 */
}

(2) body는 그대로 써도 되는데, 주석만 바꿔줘도 좋고:
body {
    font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
    background-color: #f4f1ea;
    /* height set by JS or inherits 100% */  ← 이 주석은 의미 없어졌으니 지워도 됨
    min-width: 344px;

    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: flex-start;

    padding: 10px 20px 0 20px;

    /* Safe Area */
    padding-top: max(10px, env(safe-area-inset-top));
    padding-bottom: env(safe-area-inset-bottom, 20px);
    padding-left: max(20px, env(safe-area-inset-left));
    padding-right: max(20px, env(safe-area-inset-right));

    overflow-x: hidden;
    overflow-y: auto;
    position: relative;
}


이제는:

WebView가 시스템 인셋(네비 영역)을 뺀 높이를 알아서 잡고

우리는 그냥 그 안에서 flex 레이아웃 + padding-bottom으로만 보호

✅ 3단계: 전체 화면 오버레이들도 살짝 정리

result-screen, info-popup 같은 애들이 height: 100vh라서, 폴더블에서 살짝 애매할 수 있어.

(1) 결과 화면(result-screen)

기존:

.result-screen {
    position: fixed;
    top: 0;
    left: 0;
    width: 100vw;
    height: 100vh;
    background: rgba(0, 0, 0, 0.95);
    display: none;
    z-index: 1000;
    padding: 40px 20px;
}


👉 수정:

.result-screen {
    position: fixed;
    inset: 0;              /* top:0; right:0; bottom:0; left:0 한 번에 */
    width: 100%;
    /* height: 100vh; 제거 – 뷰포트 인셋에 맞춰 자동으로 높이 결정 */
    background: rgba(0, 0, 0, 0.95);
    display: none;
    z-index: 1000;
    padding: 40px 20px;
}


그리고 안쪽 리스트는 max-height: 60vh라서 그대로 놔둬도 되고, 혹시 너무 타이트하면 나중에 조정.

(2) 게임 정보 팝업(info-popup)

기존:

.info-popup {
    position: fixed;
    top: 0;
    left: 0;
    width: 100vw;
    height: 100vh;
    background: rgba(0, 0, 0, 0.95);
    display: none;
    align-items: center;
    justify-content: center;
    z-index: 2000;
    padding: 20px;
    overflow-y: auto;
}


styles

👉 수정:

.info-popup {
    position: fixed;
    inset: 0;
    width: 100%;
    /* height: 100vh; 제거 */
    background: rgba(0, 0, 0, 0.95);
    display: none;
    align-items: center;
    justify-content: center;
    z-index: 2000;
    padding: 20px;
    overflow-y: auto;
    padding-bottom: calc(20px + env(safe-area-inset-bottom)); /* 하단 약간 여유 */
}

(3) 발자국 배경(paw-prints)은 그냥 둬도 OK
.paw-prints-container {
    position: fixed;
    top: 0;
    left: 0;
    width: 100vw;
    height: 100vh;
    pointer-events: none;
    z-index: -1;
    overflow: hidden;
}


styles

이건 완전 배경 레이어라서, 네비 뒤로 살짝 깔려 있어도 상관 없고, 굳이 safe-area를 맞출 필요는 없음.

정리하면

MainActivity에서 WindowCompat.setDecorFitsSystemWindows(window, true); 유지

index.html에서 setAppHeight 관련 JS 모두 삭제

styles.css에서

html, body 의 height: 100%; & overflow: hidden 제거

result-screen, info-popup 의 height: 100vh → 없애고 inset: 0 사용

이 상태로 다시 빌드해서:

폰 접은 상태

폰 펼친 상태(메인 화면)

가로/세로 전환

각 상태에서 리모컨 맨 아래 버튼이나 결과 화면 버튼들이 네비에 가려지지 않는지 한 번만 확인해보면 돼.